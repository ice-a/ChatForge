[package]
name = "chatforge"
version = "0.1.0"
description = "ChatForge · 会话锻造厂（Tauri 2 全平台）"
edition = "2021"
rust-version = "1.77"

[lib]
name = "chatforge_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
